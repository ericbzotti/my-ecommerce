# Local
PORT=3001
NODE_ENV=development

# CORS (URL du frontend en prod)
CORS_ORIGIN=http://localhost:5173

# DB (MySQL/Aiven)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=example
DB_NAME=ecommerce

# SSL/TLS
DB_SSL=false
# Recommandé (prod): CA en base64 (fourni par le service DB)
DB_CA_CERT_BASE64=
# Cours/dev seulement (à éviter en vrai):
DB_SSL_INSECURE=false

# Auth (si utilisé)
JWT_SECRET=change-me
